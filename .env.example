# ── Database ──
POSTGRES_USER=exp_own
POSTGRES_PASSWORD=changeme
POSTGRES_DB=expencedb

# ── Backend ──
DATABASE_URL=postgresql://exp_own:changeme@db:5432/expencedb
BACKEND_PORT=8000

# ── Frontend (Vite) ──
VITE_API_URL=http://localhost:8000
